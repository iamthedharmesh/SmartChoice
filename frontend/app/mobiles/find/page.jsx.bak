  return (
    <main className="min-h-screen bg-bg-deep">
      <div className="max-w-5xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
        <header className="mb-8">
          <div className="hero-gradient rounded-2xl border border-border-subtle px-6 py-8 sm:px-8 sm:py-10">
            <div className="inline-flex items-center gap-2 mb-3">
              <span className="text-[10px] font-semibold uppercase tracking-[0.2em] px-2.5 py-1 rounded-full bg-accent-gold/15 border border-accent-gold/30 text-accent-gold">
                SmartChoice Recommender
              </span>
            </div>
            <h1 className="text-3xl sm:text-4xl font-bold tracking-tight text-gradient-gold">
              Find Your Perfect Phone
            </h1>
            <p className="mt-3 text-base sm:text-lg text-text-secondary max-w-2xl">
              Tell SmartChoice what matters to you. Our recommendation engine compares smartphone specifications and ranks phones based on your preferences.
            </p>
          </div>
        </header>

        <div className="grid grid-cols-1 lg:grid-cols-3 gap-6">
          <div className="lg:col-span-1">
            <form ref={formRef} onSubmit={(e) => { e.preventDefault(); handleFind(); }} className="rounded-xl border border-border-subtle bg-bg-card p-5 space-y-6">
